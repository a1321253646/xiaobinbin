const { ccclass, property } = cc._decorator;
import RequiteBuilderStatusBean from "./RequiteBuilderStatusBean"
@ccclass
export default class RequiteChangeBuilder {
	user = "";
	date: RequiteBuilderStatusBean;

}